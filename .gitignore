.idea
.vscode
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.claude
.cursor
.cursorrules
.cursorrules.md
.cursorrules.txt
.cursorrules.yml
.cursorrules.yaml
.cursorrules.yml
/src_bak
.gstack/
.DS_Store

# 로컬 개발용 TLS 인증서 — 개인키가 들어간다. 절대 커밋하지 않는다.
docker/apache/certs/

# 운영 배포 산출물 (서버에만 존재한다).
# ⚠️ .gitignore 는 «줄 끝 주석»을 지원하지 않는다 — 패턴 뒤에 # 를 붙이면 통째로 무효가 된다.
# .env.deploy: MySQL root 비밀번호
# backups/    : DB 덤프 — 개인정보·위치이력이 그대로 들어 있다
# .deploy/    : 롤백 지점 기록
.env.deploy
/backups/
/.deploy/
