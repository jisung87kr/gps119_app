<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 위치 이력 보존기간 (일)
    |--------------------------------------------------------------------------
    |
    | `location_pings` 는 「누가 언제 어디 있었는지」의 이력이다. 이 앱에서 가장 민감한
    | 데이터이고(이름·전화번호보다 위험하다), 지금까지 «자동 파기가 없었다» — 쌓이기만
    | 했다(OI-7 / 에픽 Q2, 2026-08-12 에 이 값으로 확정).
    |
    | 🔑 보존기간은 «지워도 되는 기간»이 아니라 «지켜야 하는 약속»이다. 개인정보처리방침과
    |    위치기반서비스 약관에 적는 숫자와 여기가 같아야 한다. 한쪽만 바꾸면 문서가
    |    거짓말을 하게 된다.
    |
    | 왜 180일(6개월)인가: **개인정보처리방침에 이미 「실시간 위치 이력 6개월」이라고
    | 적혀 있다.** 코드가 그보다 짧게 지우면 위치정보법의 「이용·제공사실 확인자료 최소
    | 6개월 보관」과 충돌할 소지가 있고, 길게 두면 방침 위반이다. 그래서 방침에 맞춘다.
    |
    | 행사 종료 기준이 아니라 «기록 시각» 기준인 이유는 단순해서다 — 행사가 늘어나도
    | 규칙이 하나로 유지되고, 종료일이 수정되는 행사에서 보존기간이 슬며시 늘어나지 않는다.
    |
    | ⚠️ 법무 검토에서 기간이 바뀌면 **여기와 방침 문서를 같이** 고쳐야 한다.
    |    LocationRetentionTest 가 둘이 어긋나면 실패한다.
    |
    */
    'retention_days' => (int) env('LOCATION_RETENTION_DAYS', 180),

    /*
    | 한 번에 지우는 행 수. 운영 DB 에서 수백만 행을 한 문장으로 지우면 락이 오래 걸린다.
    */
    'purge_chunk' => (int) env('LOCATION_PURGE_CHUNK', 5000),

];
