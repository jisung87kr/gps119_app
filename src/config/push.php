<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 웹 푸시 (VAPID)
    |--------------------------------------------------------------------------
    |
    | 브라우저 → 서비스워커 경로. 외부 계정이 필요 없어 지금 바로 켤 수 있고,
    | 상황실은 PC 웹에서 관제하므로 앱 푸시보다 먼저 필요하다.
    |
    | 키 생성: php artisan push:vapid-keys
    | subject 는 연락 가능한 mailto: 또는 서비스 URL 이어야 한다(RFC 8292).
    |
    */

    'vapid' => [
        'subject' => env('VAPID_SUBJECT', env('APP_URL', 'http://localhost')),
        'public_key' => env('VAPID_PUBLIC_KEY'),
        'private_key' => env('VAPID_PRIVATE_KEY'),
    ],

    /*
    |--------------------------------------------------------------------------
    | FCM (앱 푸시 — iOS 는 FCM→APNs 경유)
    |--------------------------------------------------------------------------
    |
    | ⚠️ 미설정 상태가 «정상»이다. FCM 프로젝트를 만들려면 스토어·인증서 명의
    | 주체(N0-4 / M-8)가 먼저 정해져야 하고, 그건 코드로 풀 수 없는 합의 항목이다.
    | 설정되기 전까지 앱 푸시는 조용히 건너뛴다(에러 아님).
    |
    */

    'fcm' => [
        'project_id' => env('FCM_PROJECT_ID'),
        'credentials' => env('FCM_CREDENTIALS'), // 서비스 계정 JSON 파일 경로
    ],

];
