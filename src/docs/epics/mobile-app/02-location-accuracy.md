# 02. 위치 수집과 정확도

> 요구: **「사용자 위치정보 수집, 정확도 중요」**
> 이 문서의 결론: **웹 단계의 스로틀 결함은 고쳤다(§1). 앱에서 다시 실측해야 한다(§2).**

## 1. ✅ 스로틀 결함 — 발견과 수정 (2026-08-05 완료)

> 아래는 **이 에픽을 기획하며 발견해 고친 것**이다. 현재 코드에는 없는 문제이나,
> 같은 함정을 앱에서 반복하지 않기 위해 경위를 남긴다.

### 발견 당시의 사실

```php
// routes/api.php (수정 전)
// ping 수신: active 참가자(역할무관) + rate-limit(초당 1~2). 큐로 적재.  ← 주석이 틀렸다
Route::post('/events/{id}/location', [LocationApiController::class, 'store'])
    ->middleware(['event.member', 'throttle:2,1']);
```

Laravel 의 `throttle:maxAttempts,decayMinutes` 이므로 `throttle:2,1` 은 **분당 2회**다. 그런데 클라이언트는 이동 중 5초마다(분당 12회) 보내면서 주석에 *"백엔드 throttle(2/s)와 **정합**"* 이라고 적어놨다. **120배 어긋난 채 스스로 정합이라 믿고 있었다.**

실측(연속 호출):

```
0:422(limit 2) | 1:422(limit 2) | 2:429 | 3:429 | 4:429
응답 헤더: X-RateLimit-Limit: 2
```

(422 는 테스트 페이로드의 검증 실패로 **스로틀은 통과**했다는 뜻. 3번째부터 429.)

| 상태 | 클라이언트 전송 | 서버 통과 | 손실 |
|---|---|---|---|
| **이동 중** | 분당 12회 | 분당 2회 | 🔴 **10/12 (83%)** |
| 정지 | 분당 2회 | 분당 2회 | 0 |

🔑 **가장 정확도가 필요한 「이동 중」에 정확히 실패했다.** 실패 버퍼도 2개뿐이라 복구되지 않았다.

⚠️ **이 손실이 관제에서 「끊긴 마커」로 보이지 않았다** — `event_participants.last_lat/lng` 캐시가 마지막 성공 값을 유지하기 때문이다. **조용히 부정확해졌다.** 앱에서도 같은 은폐가 일어날 수 있으니, 전송 실패율을 화면 어딘가에 노출할지 검토할 것.

### 조치

| 항목 | 결과 |
|---|---|
| 스로틀 | `throttle:2,1` → **`throttle:30,1`** (이동 12회 + 재전송·복수탭 여유) |
| 클라이언트 | **429 백오프** 신설(20초 창) — 이전에는 429 를 일반 실패로 처리해 즉시 재시도하며 악화시켰다 |
| 실패 버퍼 | 2 → **12** (이동 1분치) |
| 주석 | `routes/api.php`·`locationShare.js` 두 곳 정정 + "값 변경 시 양쪽을 같이 보라" 경고 |
| 회귀 테스트 | PHP: 이동 시나리오(분당 12회)에서 429 = 0건 · **Vitest 12건** (백오프 진입·창 유지·복구·비429 구분) |

## 2. ⚠️ 앱에서 다시 정해야 하는 값

**`30/분` 은 «웹 기준» 추정치다.** 백그라운드 추적을 붙이면 전송량 특성이 달라진다.

- 포그라운드 서비스는 화면이 꺼져도 계속 보낸다 → 총량 증가
- 반대로 OS 가 위치 갱신 빈도를 조절(스로틀링)할 수 있다 → 불규칙해짐
- 여러 기기·재설치로 같은 사용자가 중복 전송할 여지

→ ~~N3 에서 실기기로 재실측한다~~ → ✅ **실측 완료(2026-09-01, iPhone 16 Pro / 운영 서버)**

| 상태 | 분당 전송 | 간격 |
|---|---|---|
| 정지(하트비트) | **~2건** | 30~42초 |
| 도보 이동 | **최다 6건** | 5~16초 |

**서버 제한 `30/분` 에 5배 여유가 있다. 조정하지 않는다.**

🔑 **적응형 주기가 설계대로 돈다는 것도 같이 확인됐다.** 첫 10m 이동이 잡히는 순간
간격이 42초 → 5초로 떨어졌다(`MOVE_THRESHOLD_M=10` · `MOVE_INTERVAL_MS=5000`).

⚠️ **도보 기준이다.** 차량 이동은 10m 를 더 빨리 넘으므로 5초 최소 간격에 붙는다 —
이론상 최대 12건/분이고, 그래도 제한의 절반이다. 다만 **여럿이 동시에** 달리는 행사에서
서버 총량이 어떻게 되는지는 이 실측이 답하지 않는다.

## 3. 웹뷰에서 위치를 어떻게 얻는가

### 3-1. 웹 `navigator.geolocation` 만으로는 부족하다

현재 `locationShare.js` 는 `watchPosition` + `enableHighAccuracy: true` 를 쓴다. 웹뷰 안에서도 동작하지만 **앱이 백그라운드로 가면 멈춘다.**

행사 현장 시나리오에서 이건 치명적이다 — 참가자는 폰을 주머니에 넣고 달리고, 구급대원은 지도 대신 통화 앱을 보고 있다. **화면이 꺼지면 위치가 끊기는 관제는 관제가 아니다.**

### 3-2. 네이티브 플러그인으로 대체

| 플랫폼 | 필요한 것 |
|---|---|
| **Android** | **Foreground Service** (`FOREGROUND_SERVICE_LOCATION`) + 상시 알림. Android 8+ 는 백그라운드 위치 취득 빈도가 제한되므로 포그라운드 서비스가 사실상 필수 |
| **iOS** | `UIBackgroundModes: ["location"]` + `allowsBackgroundLocationUpdates` + **파란 상태바 인디케이터** |

Capacitor 생태계에서는 `@capacitor/geolocation` 이 **백그라운드를 지원하지 않는다.** 백그라운드 지오로케이션 전용 커뮤니티 플러그인이 필요하다.

#### ✅ M-4 해소 (2026-08-31) — `@capacitor-community/background-geolocation`

「유지보수·라이선스·양 OS 를 반드시 실측하라」는 요구대로 **버릴 브랜치에서 실제로 붙여 빌드했다.** 후보는 둘이었다.

| | `@capacitor-community/…` | `@transistorsoft/…` |
|---|---|---|
| 라이선스 | **MIT** | CUSTOM — 릴리스 빌드는 **유료 구매** |
| Capacitor peer | `>=3.0.0` | `^8.0.0` |
| 최근 릴리스 | `1.2.26` · **2025-08-28 (1년 전)** | `9.4.0` · 2026-08-30 |

⚠️ npm 의 `time.modified` 는 커뮤니티 쪽이 2026-01 로 찍힌다. **메타데이터만 건드린 것**이고 실제 배포는 2025-08 이 마지막이다 — 그 필드만 보면 「최근까지 관리된다」로 **오독한다.**

**실측 결과 (Capacitor 8.5.0):**

| 관문 | 결과 |
|---|---|
| Android `assembleDebug` | ✅ `BUILD SUCCESSFUL` |
| iOS `xcodebuild` | ✅ `** BUILD SUCCEEDED **` (`exit=0`) |
| SPM 해석 | ✅ `capacitor-swift-pm @ 8.5.0` |
| **실제 링크** | ✅ `App.debug.dylib` 에 심볼 165개 (`addWatcher` 등). 대조군 `AppDelegate` 97개 |

🔑 **「빌드 성공」에서 멈추면 안 된다.** 플러그인이 링크에서 조용히 빠져도 빌드는 통과한다.
바이너리까지 봐야 한다 — 그리고 **`App.app/App` 이 아니라 `App.app/App.debug.dylib` 을 본다.**
Xcode 16+ 에서 앞엣것은 92KB 껍데기라 우리 `AppDelegate` 조차 안 나온다(대조군을 같이 세면
방법이 틀린 걸 바로 안다).

**무료를 고른 근거 — 「1년째 공백」은 오늘의 결함이 아니라 내일의 위험이고, 그 위험은
아래 §3-3 브리지가 이미 흡수한다.** 플러그인은 `bridge.js` 뒤에만 있으므로 나중에 깨지면
브리지 안쪽만 갈아끼우면 되고 전송 정책·권한 UX 는 안 흔들린다. **지금 돈을 쓸 근거가
실측으로는 없다.** 유료 쪽은 스파이크하지 않았다 — `^8.0.0` 을 선언하니 빌드는 당연히 되고,
확인해도 「예상대로」밖에 안 나온다.

⚠️ **되돌릴 조건을 미리 적어 둔다.** Android 16 / iOS 27 대응이 필요해졌는데 업스트림이
여전히 멈춰 있으면 그때 유료로 간다. 그 판단을 「불편해서」가 아니라 **「OS 가 요구하는데
업스트림이 안 따라온다」** 로 한다.

### 3-3. 브리지 설계 — ✅ 구현됨 (2026-08-31)

웹 코드가 Capacitor 를 직접 알지 않게 어댑터를 둔다. `locationShare.js` 는
**전송 정책(적응형 주기·버퍼·429 백오프)만 담당**하고 위치 «취득»은 위임한다.
이렇게 나눠야 셸을 바꿔도 전송 로직이 안 흔들린다.

```
resources/js/native/bridge.js           «그 앱이 이 기능을 아는가» 판정
  ├─ isNativeApp() · nativePlatform()
  └─ hasNativeCapability(BACKGROUND_LOCATION)

resources/js/native/locationTracker.js  «어떻게 얻는가» — 네이티브 어댑터
  ├─ createNativeLocationTracker(env) → 트래커 | null
  └─ toLocationPermission(raw)        → M-5 값으로 접기 (경계 매핑)

resources/js/app.js                     window.__gps119Bridge.locationTracker
public/js/components/locationShare.js   config.tracker 주입, 없으면 웹 경로
```

⚠️ **애초 설계는 `startLocationTracking()` 을 `bridge.js` «안»에 두는 것이었으나
파일을 나눴다.** `bridge.js` 는 「그 앱이 이 기능을 아는가」를 판정하는 곳이고, 거기에
플러그인 세부(`addWatcher` 옵션·payload 변환)를 섞으면 **협상 로직이 묻힌다.**
갈아끼울 때 봐야 할 곳도 흐려진다.

🔑 **웹 트래커는 브리지 쪽에 «없다».** `createNativeLocationTracker()` 는 네이티브가
가능할 때만 트래커를 주고 아니면 `null` 이다 — 웹 구현은 `locationShare.js` 안에 이미
있으므로 여기에 또 만들면 **0-8(같은 로직 두 벌 → 드리프트)이 반복된다.**

🔴 **`null` 을 주는 조건이 그 함수의 절반이다.** 웹이거나 · 플러그인이 없거나(구버전
셸) · 기능은 있다는데 `Capacitor.Plugins` 에 없으면 `null`. 여기서 잘못 트래커를 주면
**구버전 앱이 없는 플러그인을 부르다 깨진다.** 「앱이면 네이티브」가 아니라 「그 앱이
그 기능을 아는가」다.

📌 **`window` 를 거치는 이유.** `public/js/components/*` 는 브라우저에 그대로 서빙되는
모듈이고 Vite 는 번들에서 `public/` 의 JS import 를 막는다(`vitest.config.js` 주석 참조).
`import` 로 이을 수 없어서 **한 지점에서만** `window` 로 건네고, 받는 쪽은 «주입»으로
취급한다. 그래서 `locationShare.js` 는 Capacitor 를 영영 모른다.

📌 플러그인 payload 는 **W3C `GeolocationPosition` «모양»으로 바꿔** 넘긴다. 그게 이미
안쪽 계약이라 웹 경로는 무변환이고 적응은 네이티브에서만 일어난다. 권한 오류도
`err.code === err.PERMISSION_DENIED` 계약에 맞춰 접는다 — 안 그러면 권한 거부가
「알 수 없는 오류」로 표시된다.

🔑 **이 구조가 M-4 를 되돌릴 수 있게 하는 실체다.** [ADR-0008](../../adr/0008-location-permission-as-separate-axis.md)
와 §3-2 에 적은 되돌릴 조건이 실제로 오면, 갈아끼울 곳은 `locationTracker.js` 와
`bridge.js` 의 플러그인 이름 문자열 **둘뿐**이다.

⚠️ **네이티브 경로는 실기기 미검증이다.** 셸에 플러그인이 아직 없어 판정이 항상 false 이고
**지금은 언제나 웹 경로로 돈다.** 테스트(Vitest 22건)는 가짜 Capacitor 로 «계약»만
고정한 것이다 — 플러그인 배선은 셸 저장소 몫이고 N3 에 남는다.

### ✅ 3-4. 관제 지도의 이동 궤적 (M-25 — 2026-09-01 해소)

> 아래는 «해소 전» 기록이다. 지금은 좌측 패널의 「이동 궤적」 체크박스로 켜면 폴리라인이 그려진다.
> 구현은 `TrackService`(조회) · `TrackSimplifier`(솎기, 순수) · `control/trackLayer.js`(그리기).

지도는 `.participant.location` 이 올 때마다 그 사람 **마커를 옮긴다.** 지나온 자리는
남지 않는다. `kakao.maps.Polyline` 을 쓰는 코드가 **한 줄도 없고**, 위치 «이력»을 지도로
내려주는 API 도 없다(`location_pings` 를 읽는 곳은 리포트와 파기 잡뿐이다).

**데이터는 이미 다 쌓이고 있다.** 2026-09-01 실측에서 353m 이동이 `location_pings` 에
그대로 들어와 있었다 — 없는 것은 **조회 API 와 그리는 레이어**뿐이다.

⚠️ 그래서 N3 게이트 문구를 고쳤다. 원래 「관제 지도에 **궤적**이 끊김 없이 남는가」였는데
**만든 적 없는 기능을 전제**하고 있었고, 그대로 두면 「배경 추적이 안 되는 것」처럼 보인다.
게이트는 「위치가 끊기지 않는가」로 좁히고 궤적은 **M-25** 로 뺐다. (그 M-25 는 같은 날 해소됐다 — 이 절 머리말 참조.)

### ✅ 3-6. 안드로이드가 화면을 끄면 멈췄다 — 원인은 «배터리 최적화» (2026-09-01)

**실기기: Galaxy A36 (SM-A366N) / Android 16 (API 36), 운영 서버.**
화면을 끈 채 3분을 두고 서버 수신 건수를 세는 실험을 반복했다.

| 조건 | 3분간 수신 |
|---|---|
| 기본 상태 | **0건** |
| + 웹뷰 렌더러 우선순위 유지(`waivedWhenNotVisible=false`) | **0건** |
| + Capacitor 레거시 브리지(`useLegacyBridge`) | **0건** |
| **+ 배터리 최적화 예외** | ✅ **7건** (간격 22~32초) |
| 위 둘을 «되돌리고» 배터리 예외만 | ✅ **9건** (간격 10~32초, 끊김 없음) |

🔑 **원인은 배터리 최적화 하나였다.** 나머지 둘은 효과가 없어 되돌렸다.

🔴 **진단이 오래 걸린 이유를 남긴다.** 증상은 「네이티브는 도는데 좌표가 JS 에
   도달하지 않는다」로 보였다. 실제로 실험 내내 이랬다:

   - 앱 프로세스 살아 있음
   - 포그라운드 서비스 `isForeground=true`, **`types=0x8`(LOCATION)**
   - 시스템에 `ProviderRequest[HIGH_ACCURACY, WorkSource{kr.co.gps119.app}]` 등록
   - fix 도 `hAcc=6.5m` / 위성 9개로 «잡히고» 있었다

   **`dumpsys` 로 보이는 것이 전부 정상이었다.** OS 가 서비스 레코드를 남긴 채
   앱 프로세스를 재우고 있었기 때문이다. 그래서 「서비스가 죽었나」는 소거됐고
   브리지·렌더러 쪽을 먼저 의심하게 됐다.
   📌 **다음에 같은 증상을 보면 배터리 최적화를 «먼저» 본다** —
   `adb shell dumpsys deviceidle whitelist | grep <pkg>`.

⚠️ **지금 통과한 것은 adb 로 넣은 «기기 설정»이다**(`dumpsys deviceidle whitelist +pkg`).
   코드가 아니므로 **사용자 기기에는 적용되지 않는다.** 앱이 직접 예외를 요청하는
   경로가 필요하다(`REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`) → M-26.

📌 앱 대기 버킷은 `10`(ACTIVE)이었다 — standby bucket 은 원인이 아니었다.

⚠️ iOS 에는 이 개념이 없다. 같은 JS 로 iOS 가 먼저 통과한 것이 그래서였다.

### ~~3-6-old~~ (기록) N3 실패로 적었던 내용

**실기기: Galaxy A36 (SM-A366N) / Android 16 (API 36), 운영 서버.**

화면을 끈 채 3분을 두고 서버 수신 건수를 셌다. 정지 상태라도 적응형 주기상 30초마다
와야 하므로 6건 내외가 기대값이다.

```
실험 전 11:57  COUNT=28
실험 후 12:05  COUNT=28      ← 0건
마지막 ping    11:54:56 (앱이 앞에 있던 시각)
```

**네이티브는 멀쩡하다.** 같은 시각 기기에서 읽은 값:

| 확인한 것 | 값 |
|---|---|
| 앱 프로세스 | 살아 있음 |
| 포그라운드 서비스 | `isForeground=true`, **`types=0x8`(LOCATION)** |
| 시스템 위치 요청 | `ProviderRequest[@+1s0ms, HIGH_ACCURACY, WorkSource{kr.co.gps119.app}]` |
| 마지막 fix | `hAcc=6.5m`, 위성 9개 |

🔑 **그러므로 「추적이 죽은 것」이 아니라 「좌표가 JS 에 도달하지 않는 것」이다.**
`locationShare.handlePosition()` 은 좌표가 올 때마다 즉시 POST 하고 타이머에 물려
있지 않다 — 전송이 느린 게 아니라 콜백이 «안 온다».

⚠️ **iOS 는 같은 JS 로 통과했다**(2026-09-01, 353m·최대 42초). 안드로이드 고유 문제다.
📌 단서: `@capacitor-community/background-geolocation` 의 호환표는 **Capacitor v7 까지**다.
   이 앱은 v8 이다. 빌드·링크·권한·서비스는 전부 정상이므로 «깨진» 게 아니라
   «전달 경로»만 어긋난 모양이다.

📌 배터리 최적화 예외가 아니다(`dumpsys deviceidle whitelist` 에 없음). 삼성 One UI 는
   절전이 공격적이라 이것도 별도 확인이 필요하다 — 다만 서비스가 살아 있으므로
   1차 원인은 아니다.

**이건 출시 블로커다.** 백그라운드 위치는 이 앱의 존재 이유이자 App Store 4.2 소명의
핵심 근거이고, Play 백그라운드 위치 선언·시연 영상(컷 6)이 실제와 어긋나게 된다.

### 🔴 3-5. 「붙였다」와 「쓰이고 있다」는 다르다 (2026-09-01)

배경 위치를 붙이고 실기기 검증까지 마쳤는데, **운영에서 출동을 수락한 대원의 위치가
5분 만에 멈췄다.** 권한은 문제가 아니었다:

```
sharing=on   perm=always   state=stale   last_seen=5분 전
```

**「항상 허용」이 있는데도 화면을 끄자 끊겼다.** 네이티브 트래커가 애초에 쓰이지 않고
있었기 때문이다 — `createLocationSharer` 호출부가 셋인데 `tracker` 를 넘기는 곳이
**하나뿐**이었다.

| 화면 | 트래커 |
|---|---|
| `event/active` (활동) | ✅ 넘김 |
| **`dispatch/index` (지령·출동)** | 🔴 **안 넘김** ← 대원이 실제로 머무는 화면 |
| `layouts/app` (그 외 전 화면의 셸 전송기) | 🔴 안 넘김 |

즉 **대원이 있는 화면에서는 여전히 웹 `watchPosition`** 이 돌았고, 그건 화면을 끄면 죽는다.

→ **기본값을 `createLocationSharer` 안에 뒀다.** 호출부는 이제 아무것도 넘기지 않는다:

```js
config.tracker || env.__gps119Bridge?.locationTracker || webTracker(env)
```

🔑 **호출부마다 배선하면 반드시 한 곳을 빠뜨린다.** 활동 화면의 명시 배선도 지웠다 —
한 곳만 «특별»해지는 순간 그게 다음 결함의 씨앗이다.
`LocationTrackerWiringTest` 가 「어느 화면도 `tracker:` 를 직접 넘기지 않는다」를 지킨다.

> 🔴 **같은 착오를 하루에 두 번 밟았다.** 앞의 것은 셸의 네이티브 플러그인이었다 —
> 빌드에 심볼이 들어갔는데 `SceneDelegate` 가 등록 코드를 «실행하지 않아» 웹에서 안 보였다.
> **산출물에 있다 ≠ 실행 경로에 있다 ≠ 그 화면에서 쓰인다.** 층이 셋이고, 앞의 둘이
> 통과해도 마지막에서 조용히 멈춘다.

## 4. 권한 — 3단계로 나눠 요청한다

한 번에 «항상 허용»을 요구하면 거절률이 급등한다. 순서:

| 단계 | 시점 | 요청 |
|---|---|---|
| 1 | 행사 입장 직후 | **사용 중에만 허용** (`WhenInUse`) |
| 2 | 위치공유 토글 ON 또는 첫 백그라운드 필요 시점 | **항상 허용** — «왜 필요한지»를 앱 자체 화면으로 먼저 설명한 뒤 OS 다이얼로그 |
| 3 | 거부 시 | 기능 제한 안내 + 설정 앱 딥링크 |

**iOS 는 «항상 허용»을 OS 가 나중에 다시 물어본다**(재확인 프롬프트). 여기서 사용자가 «사용 중»으로 되돌리는 것을 감지해 관제에 «백그라운드 추적 불가» 상태로 표시해야 한다. 안 그러면 상황실이 «보이고 있다」고 착각한다.

#### ✅ 3단계 UX 구현됨 (2026-08-31)

| 단계 | 구현 |
|---|---|
| 1 | 입장 후 `sharer.enable()` — 기존 자동 시작이 그대로 1단계다 |
| 2 | `permissionStep === 'explain_always'` → 설명 카드 → 「항상 허용」 |
| 3 | `permissionStep === 'guide_settings'` → 제한 안내 + `openSettings()` |

판정은 `resources/js/native/locationPermission.js` 의 **순수 함수**(`decidePermissionStep`)가
하고 화면은 결과만 그린다. **화면에서 조합하면 「그럴듯하지만 틀린」 안내가 나오는데,
사람 눈으로는 안 걸린다.**

- **웹에서는 아무것도 하지 않는다** — 브라우저에 「항상 허용」이 없어서, 설명 화면을 띄우면
  «고칠 수 없는 것»을 요구하게 된다.
- 🔴 **보고가 없으면(`null`) 아무것도 하지 않는다** — 「모른다」를 「거부됨」으로 읽고 설정으로
  보내면 멀쩡한 사용자가 고칠 것도 없는 설정을 뒤진다.
- 🔴 **`denied`·`services_off` 는 공유를 켜기 «전»에도 띄운다** — 「켜고 나서야 막힌 걸 아는」
  순서면 대원은 이미 현장에 있고, 그때는 고칠 시간이 없다.
- **`when_in_use` 는 공유를 «켤 때만»** 승격을 권한다. 끄고 있는 사람에게 배경 권한을 조르면
  거절률만 올라간다.
- **`services_off` 와 `denied` 의 문구를 가른다** — 기기 위치가 꺼진 사람에게 「권한 거부」라고
  하면 앱 설정만 들여다보다 영영 못 고친다.

🔴 **재확인 프롬프트 대응**: `watchPermissionChanges()` 가 포그라운드 복귀마다 다시 읽어
서버에 보고한다. 강등은 **앱 안에서 아무 일도 일으키지 않으므로**, 복귀 시점에 읽지 않으면
관제는 끊긴 사람을 계속 「추적 중」으로 본다.

### 🔴 셸에 권한 «읽기» 메서드가 따로 있다 (M-4 플러그인의 구멍)

`@capacitor-community/background-geolocation` 의 API 는 셋뿐이다 —
`addWatcher` · `removeWatcher` · `openSettings`. **`checkPermissions()` 가 없다.**

그래서 watcher 가 `NOT_AUTHORIZED` 로 실패하는지만 알 수 있고, **「항상 허용」과
「사용 중만」을 구분할 수 없다.** 구분이 없으면 M-5 의 `tracking` 과 `foreground_only` 가
합쳐져 **관제가 «화면 닫으면 끊기는 사람»을 「추적 중」으로 본다.** 반대로 안전하게 전부
`foreground_only` 로 두면 「추적 중」이 영영 안 떠서 신호가 죽는다.

→ 셸에 **읽기 전용** 플러그인 `Gps119LocationPermission.check()` 를 뒀다.
iOS 는 `CLLocationManager.authorizationStatus`, Android 는 `ACCESS_BACKGROUND_LOCATION`
부여 여부를 그대로 읽는다. **권한을 «요청»하지는 않는다** — 요청은 배경 위치 플러그인이
하고, 요청할지 말지는 웹이 정한다(푸시와 같은 분담).

⚠️ **안드로이드는 「아직 안 물어봤다」를 못 읽는다.** 거부인지 미요청인지 구분할 API 가 없어
둘 다 `denied` 로 답한다. `blocksTracking()` 이 둘을 같게 취급하므로 관제 판정은 같고,
사용자 안내 문구만 덜 정확해진다.

#### ✅ 실기기 검증 완료 (2026-08-31, iPhone 16 Pro / iOS 26.6.1)

```
perm=when_in_use  →  state=foreground_only   (앱 열려 있을 때만)
설정에서 「항상」으로 변경 → 앱 복귀
perm=always       →  state=tracking          (추적 중)
```

🔑 **설정에서 바꾼 것을 앱이 «복귀만으로» 감지했다.** `watchPermissionChanges()` 가
실제로 도는 것을 확인한 것이고, 이는 M-5 의 핵심 시나리오(iOS 재확인 프롬프트로
「사용 중」으로 강등되는 것을 잡기)를 **반대 방향으로 검증한 셈**이다.

⚠️ **두 행사에 참가한 사람은 «화면을 연 행사»만 갱신된다.** 실측에서 project 6 은
`always`, project 7 은 `when_in_use` 로 남았다. 권한을 (행사, 참가자)에 저장하기로 한
[ADR-0008](../../adr/0008-location-permission-as-separate-axis.md) D1 의 파생 결과다 —
관제가 다른 행사를 볼 때 낡은 값을 볼 수 있다.

⚠️ **iOS 「사용 중 → 항상」 승격 프롬프트는 앱당 사실상 한 번만 뜬다.** 이미 소진됐으면
`requestAlwaysAuthorization()` 을 불러도 **아무 일도 일어나지 않는다**(오류도 없다).
그때는 설정에서 직접 바꾸는 수밖에 없고, 3단계 안내가 그래서 필요하다.

### 🔴 실기기에서만 드러난 것 다섯 (2026-08-31)

**전부 테스트가 통과하는 상태에서 안 돌았다.** 하루의 대부분이 이걸 찾는 데 들어갔다.

| # | 무엇 | 증상 |
|---|---|---|
| 1 | 네이티브 플러그인 등록 4관문 (아래) | 플러그인이 웹에 «안 보임» |
| 2 | `Capacitor.Plugins` 접근 방식 오해 | 트래커가 조용히 `null` — 배경 위치가 한 번도 안 돎 |
| 3 | `requestPermissions` 를 넘길 «수단» 부재 | 「항상 허용」 버튼이 프롬프트를 영영 안 띄움 |
| 4 | 공유 토글 변경 시 단계 재계산 누락 | 카드가 사라진 채 안 돌아옴 |
| 5 | `public/` 원본 서빙 모듈에 캐시 버스팅 없음 | **「버튼이 안 눌린다」** — 3시간 전 JS 가 돌고 있었다 |

🔑 **5번이 가장 고약하다 — 증상이 UI 문제처럼 보인다.** `/js/components/*` 는 Vite 번들이
아니라 원본 그대로 서빙돼 해시가 안 붙는다. 파일을 고쳐도 웹뷰가 옛 사본을 계속 쓴다.
**같은 위험이 8곳에 더 있다**(`RequestMapApp.js` · `RequestShowApp.js` · `kakaoNavi.js` 등).

🔑 **진단은 「화면에 한 줄 띄우기」로 뚫렸다.** 서버 로그만으로는 어느 «층»에서 끊기는지
가릴 수 없었다. `plugins=` 목록 한 줄이 「네이티브 등록 실패」를 즉시 확정했다.

🔴 **`event_participants.sharing_location` 은 앱 권한 상태와 별개다.** **«공유 켬 + OS 권한 없음»** 상태를 구분해 표현해야 한다.

#### ✅ M-5 해소 (2026-08-31) — 권한을 별도 축으로, 상태는 서버에서 파생 → [ADR-0008](../../adr/0008-location-permission-as-separate-axis.md)

관제가 알아야 할 것은 셋인데 둘밖에 없었다.

| 축 | 질문 | 이전 |
|---|---|---|
| 의도 | 사용자가 켰는가 | `sharing_location` — **있지만 관제로 안 내려갔다** |
| 능력 | OS 가 허락했는가 | 🔴 **없음** |
| 증거 | 실제로 들어오는가 | `last_seen_at` → `online` |

그래서 「껐다」·「권한이 없다」·「네트워크가 끊겼다」가 전부 같은 «오프라인»으로 보였다.
**포그라운드 공유에서는 「화면 보고 있으니 곧 돌아오겠지」로 넘어갔지만, 배경 추적은
«안 보고 있어도 보인다»가 전제라 그 가정이 무너진다.**

서버 구현이 끝났다 — `location_permission` 컬럼, `LocationPermission`/`TrackingState` enum,
`PATCH /api/events/{id}/location-permission`, roster 의 `tracking_state`. 테스트 15건.

⚠️ **셸의 보고 배선은 N3 몫이다.** 앱이 보고하지 않으면 전원 `unknown`/`off` 로 남는다 —
서버만 배포해서는 화면이 달라지지 않는다. 관제 UI 배지도 아직이다.

🔴 **`unknown` 은 「문제 없음」이 아니라 「모름」이다.** 웹 사용자가 다수인 동안은 관제
화면 대부분이 `unknown` 이다. 이걸 정상처럼 칠하면 M-5 를 다시 잃는다.

## 5. 정확도 설계

| 항목 | 값 | 근거 |
|---|---|---|
| `enableHighAccuracy` | true (유지) | 산길·코스에서 네트워크 측위로는 부족 |
| 이동 판정 | 10m (`MOVE_THRESHOLD_M`, 유지) | |
| 이동 중 전송 | 5초 (유지 — 서버 스로틀 상향으로 실효화됨) | |
| 정지 하트비트 | 30초 (유지) | |
| **정확도(accuracy)** | ✅ 수집→저장→**화면**까지 연결 완료 (아래) | 정확도 100m 짜리 픽스를 10m 짜리와 같이 그리면 관제가 틀린 확신을 갖는다 |
| **배터리** | 포그라운드 서비스 상시 = 배터리 소모 큼 | 행사 시간(수 시간) 기준으로 실측 필요 |

### ✅ accuracy 파이프라인 — 2026-08-05 수정 완료

발견 당시 파이프라인이 **두 칸에서 끊겨 있었다.**

| 구간 | 발견 시 | 현재 |
|---|---|---|
| 클라이언트 전송 | ✅ `locationShare.js:125` | ✅ |
| 요청 검증 | ✅ `StoreLocationPingRequest.php:25` | ✅ |
| 이력 적재 | ✅ `location_pings.accuracy` | ✅ |
| 브로드캐스트 페이로드 | ✅ `ParticipantLocationUpdated` (원래 있었음) | ✅ |
| **참가자 캐시** | 🔴 없음 | ✅ `event_participants.last_accuracy` 추가 |
| **관제 roster 응답** | 🔴 없음 | ✅ `rosterForControl` 에 포함 |
| **관제 화면 표시** | 🔴 사용처 0건 | ✅ 마커 인포윈도에 오차 반경 |

🔑 **끊긴 지점이 「최초 로드」였다.** 브로드캐스트에는 accuracy 가 실려 있었지만 캐시에 없어서 **관제가 처음 켜질 때 받는 roster 에는 빠져 있었고**, SPA 는 실시간으로 들어온 값도 쓰지 않고 버렸다. 결과적으로 오차 5m 인 사람과 500m 인 사람이 «똑같은 점»으로 찍혔다. 산에서 500m 는 사람을 못 찾는 거리다.

**표시 규칙** (`markerPool.js`):

| 오차 | 표기 |
|---|---|
| < 50m | 초록 `오차 반경 ±12m` |
| 50~150m | 주황 |
| ≥ 150m | 빨강 `오차 반경 ±300m · 신뢰 낮음` |
| 값 없음 | 회색 **`정확도 정보 없음`** — 빈칸으로 두면 «정확하다»로 읽힌다 |

> 남은 것: 지도에 **오차 반경 원(circle)** 표시는 넣지 않았다. 마커 수가 많을 때 렌더 비용을 재보고 결정한다.

## 6. 개인정보 — 앱은 요구 수준을 올린다

- 🔴 **`location_pings` 자동 파기 정책이 아직 없다**(선행 에픽 Q2/OI-7 미결). **백그라운드 상시 수집이 붙으면 데이터량과 민감도가 동시에 올라간다.** 스토어 심사에서도 보관 기간을 묻는다
- 두 스토어 모두 **위치 데이터 수집·목적·제3자 제공 여부 신고**가 필수 → [`05-store-release.md`](05-store-release.md)
- 앱 내 **위치 이력 조회·삭제 요청 경로**를 제공할 것인지 결정 필요(M-6)

### 🔴 6-1. 「파기」로만 접근하면 틀린다 — 법이 «남기라»는 것이 따로 있다 (2026-08-05 검토)

지금까지 이 항목은 **«보존기간을 정하고 지운다»** 로만 프레이밍돼 있었고, [`05 §1-2`](05-store-release.md) 도 짧은 보관을 심사 방어 논리로 봤다. **그 방향만으로는 위험하다.**

「위치정보의 보호 및 이용 등에 관한 법률」은 위치정보사업자등에게 **위치정보 수집·이용·제공 «사실 확인자료»를 위치정보시스템에 자동 기록·보존할 의무**를 지우고, 시행령이 **최소 보관기간(6개월)** 을 둔다. 즉 **파기 잡을 짜기 전에 두 가지를 갈라야 한다.**

| 구분 | 무엇 | 방향 |
|---|---|---|
| **궤적 원본** | `location_pings` 의 좌표·정확도 시계열 | **파기 대상.** 보존기간을 짧게 정하는 게 맞다 |
| **제공사실 확인자료** | «누구의 위치를, 언제, 누구에게 제공했는가»의 기록 | 🔴 **보존 의무 대상.** 지우면 안 된다 |

⚠️ **지금 설계에는 후자가 아예 없다.** `location_pings` 에 **위치를 기록한 시각(`recorded_at`)은 있다** — 없는 것은 Laravel `created_at`/`updated_at` 뿐이다([마이그레이션](../../../database/migrations/2026_06_26_100001_create_location_pings_table.php)). 문제는 시각이 아니라 **«누구에게 제공했는가»** 다: 관제·구급대에게 브로드캐스트한 «제공 사실»을 남기는 테이블이 따로 없다. **0-3 을 지금 문서대로 실행하면 «법이 남기라는 것까지 지우는 파기 잡»이 나온다.**

🔑 **M-15(법무 확인)에 이걸 같이 물어야 한다** — 「신고 대상인가」만이 아니라 **「우리가 남겨야 하는 자료가 무엇인가」**. 답에 따라 M-6 의 산출물이 «파기 잡 하나»가 아니라 **«파기 잡 + 제공이력 테이블»** 두 개가 된다.

## 7. 미결

| ID | 질문 |
|---|---|
| ~~M-4~~ | ~~백그라운드 지오로케이션 플러그인 선정~~ → ✅ **해소(2026-08-31)** — `@capacitor-community/background-geolocation`(MIT). 실측 근거는 위 §3-2 |
| ~~M-5~~ | ~~「공유 켬 + OS 권한 없음」 데이터 모델~~ → ✅ **해소(2026-08-31)** — [ADR-0008](../../adr/0008-location-permission-as-separate-axis.md). **관제 UI 표시와 셸 보고 배선은 N3 에 남는다** |
| M-6 | 위치 이력 보존기간·자동파기(선행 에픽 Q2 승계) · 앱 내 삭제 요청 경로 |
| M-7 | 배터리 소모 실측 기준(행사 4시간 기준 허용치) |
